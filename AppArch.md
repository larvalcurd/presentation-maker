# Архитектура проекта Presentation Maker (React + TypeScript + Redux)

## 📁 src/
Основная директория проекта, где лежит весь исходный код приложения.

---

## 📁 app/
Отвечает за глобальную инфраструктуру приложения.

- **store/** – Redux store.
  - `index.ts` – создание store и подключение middleware.
  - `rootReducer.ts` – объединение всех слайсов приложения (presentation, slides, editor и т.д.).
- **hooks/** – кастомные хуки.
  - Примеры: `useAppSelector`, `useAppDispatch`, `useUndoRedo`.
- `App.tsx` – корневой компонент приложения. Подключение роутера и глобальных провайдеров (Redux, ThemeProvider).

---

## 📁 features/
Содержит фичи/доменные области проекта. Каждая фича инкапсулирует свою логику, визуальные компоненты и утилиты.

### 📁 presentation/
Управление презентацией (название, структура, список слайдов).

- **components/** – презентационные компоненты.
  - `TitleEditor.tsx` – редактирование названия презентации.
  - `SlideListPreview.tsx` – превью слайдов.
- **model/** – Redux slice, типы и selectors для презентации.
- **utils/** – вспомогательные функции (например, `reorderSlides`).

### 📁 slides/
Работа с отдельными слайдами.

- **components/** – визуальные компоненты слайда.
  - `SlideThumbnail.tsx`, `SlideCanvas.tsx`, `SlideToolbar.tsx`.
- **model/** – slice состояния слайда, типы и selectors.
- **utils/** – функции для работы с объектами на слайде (resize, move, addObject).

### 📁 editor/
Рабочая область редактирования.

- **components/** – визуальные элементы редактора.
  - `Canvas.tsx`, `ObjectHandles.tsx`, `Toolbar.tsx`.
- **model/** – slice состояния выделений, undo/redo.
- **utils/** – расчёты координат, преобразования объектов.

### 📁 export/
Экспорт презентации.

- **model/** – slice состояния экспорта (если нужно).
- **utils/** – функции `convertToPDF`, `generateFileName`.

### 📁 storage/
Сохранение и загрузка JSON.

- **model/** – slice состояния сохранения.
- **utils/** – `fileSave`, `fileLoad`, адаптеры.

---

## 📁 shared/
Содержит универсальные вещи, которые можно использовать в любом месте проекта.

- **ui/** – базовые UI-компоненты: `Button`, `Input`, `Modal`, `ColorPicker`.
- **lib/** – утилиты: `uuid`, `debounce`, `downloadFile`.
- **constants/** – константы проекта: цвета, размеры, настройки.
- **types/** – глобальные TypeScript типы: `Slide`, `Presentation`, `ObjectOnSlide`.
- **styles/** – глобальные стили, темы, CSS-переменные.

---

## 📄 main.tsx
Точка входа приложения. Монтирует `App.tsx` и подключает Redux Provider.

---

## 📄 vite-env.d.ts
Типы для Vite + TypeScript.

---

> 🔹 Такая структура обеспечивает чёткое разделение:  
> - **app/** – глобальная инфраструктура;  
> - **features/** – бизнес-логика и UI по фичам;  
> - **shared/** – переиспользуемые компоненты, утилиты, типы и стили.  

Это облегчает поддержку, масштабирование и коллаборацию с командой.
